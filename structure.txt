taskmaster/
├── backend/
│   ├── src/
│   │   ├── api/             # Ingestion webhooks & dashboard endpoints (FastAPI/Express)
│   │   │   └── routes/
│   │   ├── services/
│   │   │   ├── ai/          # LLM prompt templates, parsers, classifiers
│   │   │   ├── integrations/# Gmail, Twilio, Google Calendar, Stripe connectors
│   │   │   └── tickets/     # Database CRUD and ticket generation logic
│   │   ├── models/          # Data schemas (Pydantic / TypeScript types)
│   │   └── config.py        # Environment variables & API credentials
│   ├── tests/
│   └── requirements.txt     # (or package.json if Node/TS)
├── frontend/                # Lightweight UI / Technician dashboard
│   ├── src/
│   │   ├── components/      # Ticket list, status badges, details view
│   │   └── pages/
│   └── package.json
├── docs/                    # PRD, architecture diagrams, demo flow
├── .env.example             # Template for API keys (OpenAI/Gemini, Webhooks)
├── .gitignore
├── docker-compose.yml       # Quick local spin-up for backend + database
└── README.md